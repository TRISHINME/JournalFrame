{
  "entities": {
    "JournalEntry": {
      "title": "Journal Entry",
      "description": "User journal entry with conversational Gemini response and cognitive bias analysis.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the journal entry."
        },
        "uid": {
          "type": "string",
          "description": "The UID of the authenticated user who owns this journal entry."
        },
        "message": {
          "type": "string",
          "description": "The user's journal reflection or prompt."
        },
        "reply": {
          "type": "string",
          "description": "Gemini 2.5 Flash conversational response."
        },
        "summary": {
          "type": "string",
          "description": "Concise 2-sentence summary of the entry."
        },
        "cognitiveAnalysis": {
          "type": "object",
          "description": "Cognitive bias, sentiment, and reframing analysis."
        },
        "createdAt": {
          "type": "string",
          "description": "ISO timestamp of creation."
        }
      },
      "required": ["id", "uid", "message", "reply", "createdAt"]
    }
  },
  "firestore": {
    "/journals/{uid}/entries/{entryId}": {
      "schema": "JournalEntry",
      "description": "Strictly tenant-isolated journal logs for each authenticated user."
    }
  }
}
